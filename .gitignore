# Local secrets and runtime state
.env
events.ini
.log/
.logs/
.temp/
.cache/

# IDE/cache
.vs/
__pycache__/
**/__pycache__/
*.pyc
*.user
*.suo

# Local tools are installed outside Git; keep only the installation note.
tools/**
!tools/
!tools/README.txt
# build.bat est versionne et APPELLE ce script : sans lui, un clone frais ne
# peut pas construire de release. Il ne contient que des chemins relatifs.
!tools/release-framework-dependent.ps1
!tools/**/
!tools/**/README.txt

# mem-curator : outil interne privé - hors du repo public (historique purgé le 2026-08-09).
# L'exclusion du dossier entier prime sur les ré-inclusions tools/**/ ci-dessus.
tools/mem-curator/

# Product specs / cahiers des charges stay local.
CAHIER_DES_CHARGES*
*CDC*

# Docs: keep only images and PDFs.
docs/**
!docs/**/
!docs/**/*.pdf
!docs/**/*.png
!docs/**/*.jpg
!docs/**/*.jpeg
!docs/**/*.gif
!docs/**/*.webp
!docs/**/*.bmp
!docs/**/*.svg
!docs/**/*.ico

# Legacy/manual snapshots
.archive/
**/.archive/
.versioning/

# Build outputs
bin/
obj/
src/**/bin/
src/**/obj/
src/**/.vs/
src/**/.idea/
src/**/TestResults/
src/**/coverage/
src/**/*.user
src/**/*.suo
src/**/*.pdb
src/**/*.cache
src/**/GeneratedFiles/
temp/
temp_publish*/
artifacts/
/projects-source/
/media/
/package-installer/

# Runtime logs and generated caches
logs/
*.log
*.jsonl

# Backup/generated binaries
*.bak

# Root runtime binaries (shipped via GitHub Releases, not source control)
/RetroBat.Api.exe
/RetroBat.Api.Update.exe
/RetroBat.Api.deps.json
/RetroBat.Api.runtimeconfig.json
/RetroBat.Api.xml
/web.config
/*.pdb

# APIExpose Data Pack (protected by DATA-LICENSE.md, shipped via GitHub Releases)
/resources/

# MkDocs build output
site/

# Runtime state (panel-remap ownership registry, etc.)
/state/

# Curator : savoir-faire prive, jamais dans le repo public (historique purge le 2026-07-06)
panel_curator_ultimate.py
panel_curator.ini


panel_curator_ultimate*
publish-tmp/
dist/
/RetroBat.Api.Update.exe.old
